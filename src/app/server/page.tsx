
export default async function server() {

  // const session = await getServerSession(options);

  // if (!session) 
  //   redirect('/api/auth/signin')
 
  return (
    <div className='h-screen flex justify-center items-center'>
        <h1 className='text-5xl'>Server Page</h1>
    </div>
  )
}
